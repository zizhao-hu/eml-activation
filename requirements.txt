# Pinned versions known to work (sandbox where the paper's tables were produced).
# CPU-only is fine; CUDA wheels work too — pip will pick the right one.
torch>=2.1,<3
numpy>=1.24,<3
tqdm>=4.66,<5
pyyaml>=6.0,<7
matplotlib>=3.8,<4

# dev
pytest>=8.0,<9
